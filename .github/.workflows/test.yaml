name: my first workflow

on: [push]
jobs:
  run-shell-command:
  runs-on: ubuntu-latest
    steps:
      - name: acho a string
        run: echo "Hello World"
      - name: nultiline script
        run: |
            node -v
            npa -v